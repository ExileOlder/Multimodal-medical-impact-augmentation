# Core runtime for the finalized Stage A 1K RetinaLogos adapter.
# The full conda export used on the training server is kept at:
#   codes/environment_RetinaLogos.yml

torch>=2.5.0
torchvision>=0.20.0
numpy>=1.24.0
Pillow>=10.0.0
PyYAML>=6.0.0
tqdm>=4.65.0
einops>=0.7.0
scipy>=1.10.0
opencv-python>=4.8.0
scikit-image>=0.21.0
pandas>=2.0.0

diffusers>=0.32.0
transformers>=4.45.0
accelerate>=1.2.0
fairscale>=0.4.13
safetensors>=0.4.0

gradio>=5.0.0
matplotlib>=3.8.0

# Optional but recommended on Ampere/Ada GPUs.
# Install manually when the CUDA toolchain is ready:
#   pip install flash-attn --no-build-isolation
